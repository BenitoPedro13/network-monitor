# Agent Guidelines

This repo's source of truth for project workflow is `CLAUDE.md`.

For Codex or other agent sessions:

- Read `CLAUDE.md` and `ARCHITECTURE.md` before changing code.
- Follow the task-doc workflow in `docs/tasks/`.
- Do not mark tasks `Done` until the user explicitly confirms verification.
- Keep this file as a pointer, not a fork of the project rules, so agent guidance stays in one place.
