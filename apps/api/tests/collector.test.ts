import { describe, it } from "node:test";
import assert from "node:assert/strict";

// Placeholder — unit tests for collector modules will be added as each module is built.
// Each module gets its own describe block in this file or a sibling file.

describe("collector", () => {
  it("test runner is wired up correctly", () => {
    assert.equal(1 + 1, 2);
  });
});
