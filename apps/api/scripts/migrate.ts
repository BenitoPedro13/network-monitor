const migrationSteps: readonly string[] = [
  "[db:migrate] Placeholder migration script.",
  "[db:migrate] Replace this with Prisma/Knex/TypeORM commands."
];

for (const step of migrationSteps) {
  console.log(step);
}
